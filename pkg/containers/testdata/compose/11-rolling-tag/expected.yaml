name: myapp
services:
  myapp:
    container_name: myapp
    image: ghcr.io/trueforge-org/myapp:rolling
    ports:
      - mode: ingress
        host_ip: 127.0.0.1
        target: 8080
        published: "8080"
        protocol: tcp
    restart: unless-stopped
